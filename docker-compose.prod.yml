version: "3.8"

services:
  # Next.js Frontend
  frontend:
    image: ghcr.io/liutauras-m/conusai-rankonai-frontend:20251216-0312
    environment:
      - NODE_ENV=production
      - BACKEND_URL=http://backend:8000
      - NEXT_PUBLIC_TURNSTILE_SITE_KEY=${NEXT_PUBLIC_TURNSTILE_SITE_KEY}
      - TURNSTILE_SECRET_KEY=${TURNSTILE_SECRET_KEY}
      - NEXT_PUBLIC_POSTHOG_KEY=${NEXT_PUBLIC_POSTHOG_KEY}
      - NEXT_PUBLIC_POSTHOG_HOST=${NEXT_PUBLIC_POSTHOG_HOST:-https://eu.i.posthog.com}
    depends_on:
      - backend
    labels:
      - traefik.enable=true
      - traefik.docker.network=dokploy-network
      # HTTP -> HTTPS redirect
      - traefik.http.middlewares.rankon-redirect.redirectscheme.scheme=https
      - traefik.http.middlewares.rankon-redirect.redirectscheme.permanent=true
      - traefik.http.routers.rankon-web.rule=Host(`rankon.cloud.conusai.com`)
      - traefik.http.routers.rankon-web.entrypoints=web
      - traefik.http.routers.rankon-web.middlewares=rankon-redirect
      # HTTPS router
      - traefik.http.routers.rankon-websecure.rule=Host(`rankon.cloud.conusai.com`)
      - traefik.http.routers.rankon-websecure.entrypoints=websecure
      - traefik.http.routers.rankon-websecure.tls=true
      - traefik.http.routers.rankon-websecure.tls.certresolver=letsencrypt
      - traefik.http.routers.rankon-websecure.service=rankon
      - traefik.http.services.rankon.loadbalancer.server.port=3000
    networks:
      - dokploy-network
    restart: unless-stopped

  # FastAPI Backend (internal)
  backend:
    image: ghcr.io/liutauras-m/conusai-rankonai-backend:20251216-0312
    environment:
      - REDIS_URL=redis://redis:6379
      - MAX_CONCURRENT_ANALYSES=5
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - XAI_API_KEY=${XAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    depends_on:
      - redis
    networks:
      - dokploy-network
    restart: unless-stopped

  # Redis
  redis:
    image: redis:alpine
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - dokploy-network
    restart: unless-stopped

  # TaskIQ Worker
  worker:
    image: ghcr.io/liutauras-m/conusai-rankonai-backend:20251216-0312
    command: taskiq worker tasks.taskiq_worker:broker --fs-discover
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://redis:6379
      - MAX_CONCURRENT_ANALYSES=5
      - CACHE_TTL=3600
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - XAI_API_KEY=${XAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    depends_on:
      - redis
    networks:
      - dokploy-network
    restart: unless-stopped

volumes:
  redis_data:

networks:
  dokploy-network:
    external: true